# inha-v1

AI 시대의 업무방식 — 정적 HTML 프레젠테이션.

## 배포

`main` 브랜치 푸시 시 GitHub Actions가 `AI_시대의_업무방식.html`을 `index.html`로 GitHub Pages에 배포합니다.

워크플로: [`.github/workflows/deploy.yml`](.github/workflows/deploy.yml)
